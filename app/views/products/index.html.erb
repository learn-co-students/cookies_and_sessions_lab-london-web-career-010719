<h1>Your Cart</h1>

<h4>Your Current Items</h4>

<% if session[:cart] %>
  <ul>
    <% session[:cart].each do |item| %>
      <li>
        <%= item %>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>You have no items - add some to get started</p>
<% end %>


<h4>Add another item:</h4>
<%= form_tag products_add_path, method: :post do %>

  Add an item:
  <%= text_field_tag :product %>

  <%= submit_tag "add to cart"%>

<% end %>
